const defaultState = {
  user: {
    "name": "Leanne Graham",
    "username": "Bret",
    "email": "Sincere@april.biz",
  },
}

export const userReduser = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}